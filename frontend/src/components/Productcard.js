import { withRouter } from 'react-router-dom'
import React from 'react'

const Character = (props) => {
console.log(props)
  return (
    
    <div>
     esto ya no sirve ni parece afectar
    </div>
  )
}

export default withRouter(Character)
